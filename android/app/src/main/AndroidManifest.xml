<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- API 30+ package-visibility: lets CustomTabsIntent (SignInScreen's
         "Continue with Google") find an installed Custom Tabs provider
         instead of silently degrading straight to a plain browser launch. -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneTownCity"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom-scheme deep links used by in-app navigation (see
                 MainActivity.kt's per-tab `deepLink` field, e.g.
                 onetowncity://students/{categoryKey}). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="onetowncity" />
            </intent-filter>

            <!-- Real App Links (autoVerify) — requires
                 https://onetowncity.com/.well-known/assetlinks.json to list
                 this app's release signing certificate fingerprint. That
                 endpoint is served by core.views.assetlinks_json (added in
                 Phase 6 — a prior version of this comment claimed it already
                 existed; it didn't). A real production keystore now exists
                 (~/.android/keystores/onetowncity-release.jks, wired into
                 this module's release signingConfig) and its fingerprint is
                 set in the local .env's ANDROID_RELEASE_CERT_SHA256 — but
                 App Links verification only actually passes once whichever
                 environment serves production traffic for onetowncity.com
                 has that same value set in ITS OWN environment config, which
                 is outside this repository. No path restriction: Compose
                 Navigation's per-destination navDeepLink() patterns decide
                 which screen a given path opens once the OS has routed the
                 Intent here. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="onetowncity.com" />
                <data android:host="www.onetowncity.com" />
            </intent-filter>
        </activity>
    </application>
</manifest>
